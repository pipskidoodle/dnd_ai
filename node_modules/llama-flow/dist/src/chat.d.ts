import { Model } from './models/interface';
import type { ChatConfig, ChatRequestOptions, ChatResponse, Message, Persona, RawPrompt } from './types';
export declare class Chat {
    persona: Persona;
    config: ChatConfig;
    model: Model;
    messages: Message[];
    constructor(persona: Persona, config: ChatConfig, model: Model);
    request<T>(prompt: RawPrompt<T>, opt?: ChatRequestOptions): Promise<ChatResponse<T>>;
    reset(): void;
}
//# sourceMappingURL=chat.d.ts.map