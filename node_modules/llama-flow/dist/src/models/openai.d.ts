import { Chat } from '../chat';
import type { ChatConfig, ChatRequestOptions, ChatResponse, Message, ModelConfig, OpenAIConfig, Persona } from '../types';
import type { Model } from './interface';
export declare class OpenAI implements Model {
    private openai;
    private defaults;
    constructor(config: OpenAIConfig, defaults?: ModelConfig);
    chat(persona: Persona, config?: ChatConfig): Chat;
    request(messages: Message[], config?: Partial<ModelConfig>, { retries, retryInterval, timeout, ...opt }?: ChatRequestOptions): Promise<ChatResponse<string>>;
}
//# sourceMappingURL=openai.d.ts.map